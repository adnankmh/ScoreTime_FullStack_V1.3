@extends('layouts.app')
@section('content')
<div class="container py-4"><div class="d-flex justify-content-between align-items-end mb-4"><div><span class="badge bg-primary mb-2">PLAYERS</span><h1 class="display-6 fw-bold mb-0">Global Player Directory</h1></div><a href="{{route('world.index')}}" class="btn btn-outline-secondary">World Football</a></div>
<form class="mb-4"><div class="input-group input-group-lg"><input class="form-control" name="q" value="{{$q}}" placeholder="Search real players"><button class="btn btn-primary">Search</button></div></form>
<div class="row g-3">@forelse($players as $p)<div class="col-md-6 col-xl-4"><a class="card p-3 h-100 text-decoration-none text-reset" href="{{route('player.show',['player'=>$p->slug])}}"><div class="d-flex align-items-center gap-3">@if($p->photo_url)<img src="{{$p->photo_url}}" width="62" height="62" class="rounded-circle" style="object-fit:cover">@else<div class="rounded-circle bg-body-secondary" style="width:62px;height:62px"></div>@endif<div class="flex-grow-1"><div class="fw-bold">{{$p->name}}</div><div class="small text-muted">{{$p->team?->name_en}} • {{$p->position}} • {{$p->nationality}}</div></div>@if($p->rating)<span class="badge bg-success">{{number_format($p->rating,1)}}</span>@endif</div></a></div>@empty<div class="col-12"><div class="alert alert-info">No synchronized players yet.</div></div>@endforelse</div><div class="mt-4">{{$players->links()}}</div></div>
@endsection
