@extends('layouts.app') @section('content')<div class="admin-head"><div><span class="eyebrow">MATCH CENTER</span><h1>{{__('ui.matches')}}</h1></div><form><input type="date" name="date" value="{{request('date',now()->toDateString())}}" onchange="this.form.submit()"></form></div><div class="match-grid">@forelse($matches as $m)<a class="match-card" href="{{route('match.show',$m)}}"><div class="pill">{{$m->competition?->name_ar}}</div><p class="muted">{{$m->kickoff_at?->format('H:i')}} · {{$m->venue}}</p><h3>{{$m->homeTeam?->name_ar}} <span class="score">{{$m->home_score}} - {{$m->away_score}}</span> {{$m->awayTeam?->name_ar}}</h3>@if(in_array($m->status,['live','halftime']))<span class="live">● {{$m->minute}}' {{__('ui.live')}}</span>@else<span class="muted">{{$m->status}}</span>@endif</a>@empty<div class="panel">No matches for this date.</div>@endforelse</div>@endsection
