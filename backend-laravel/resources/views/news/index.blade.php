@extends('layouts.app') @section('content')<div class="admin-head"><div><span class="eyebrow">EDITORIAL</span><h1>{{__('ui.news')}}</h1></div></div><div class="news-grid">@foreach($articles as $a)<a class="news-card" href="{{route('news.show',$a->slug)}}"><span class="pill">{{$a->category}}</span>@if($a->is_breaking)<span class="live"> BREAKING</span>@endif<h3>{{$a->title}}</h3><p class="muted">{{$a->excerpt}}</p><small class="muted">{{$a->published_at?->diffForHumans()}}</small></a>@endforeach</div><div style="margin-top:20px">{{$articles->links()}}</div>@endsection
