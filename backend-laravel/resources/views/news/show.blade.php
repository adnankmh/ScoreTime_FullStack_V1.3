@extends('layouts.app') @section('content')<article class="panel" style="max-width:900px;margin:auto"><span class="pill">{{$article->category}}</span>@if($article->is_breaking)<span class="live"> BREAKING</span>@endif<h1 style="font-size:clamp(2rem,5vw,4rem);line-height:1.12">{{$article->title}}</h1><p class="muted">{{$article->author_name}} · {{$article->published_at?->format('Y-m-d H:i')}} · {{$article->views}} views</p><p style="white-space:pre-line;font-size:1.08rem">{{$article->body}}</p></article>@endsection
