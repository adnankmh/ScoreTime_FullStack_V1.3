@extends('layouts.admin') @section('content')<div class="admin-head"><div><span class="eyebrow">V0.3 GLOBAL</span><h1>Fan & market operations</h1></div><span class="pill">{{$predictions}} predictions</span></div><div class="security-grid"><section class="panel"><h2>Transfer feed</h2>@foreach($transfers as $t)<div class="result-row"><b>{{$t->player?->name}}</b><span>{{$t->fromTeam?->name}} → {{$t->toTeam?->name}}</span></div>@endforeach</section><section class="panel"><h2>Fan Room moderation</h2>@foreach($fanMessages as $m)<form class="result-row" method="post" action="{{route('admin.fan-messages.moderate',$m)}}">@csrf @method('PATCH')<span>{{$m->body}}</span><button class="btn ghost">{{$m->status}}</button></form>@endforeach</section></div>@endsection
