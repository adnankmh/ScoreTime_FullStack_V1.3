@extends('layouts.app')
@section('content')<div class="container py-4"><h1>Media Library</h1><form method="post" enctype="multipart/form-data" action="{{ route('admin.media.store') }}" class="card card-body mb-4">@csrf<div class="row g-2"><div class="col-md-4"><input class="form-control" type="file" name="file" required></div><div class="col-md-3"><input class="form-control" name="alt_text" placeholder="Alt text"></div><div class="col-md-2"><input class="form-control" name="credit" placeholder="Credit"></div><div class="col-md-2"><input class="form-control" name="license" placeholder="License"></div><div class="col-md-1"><button class="btn btn-primary w-100">Upload</button></div></div></form><div class="row g-3">@foreach($assets as $a)<div class="col-6 col-lg-3"><div class="card h-100"><div class="card-body"><div class="small text-secondary text-break">{{ $a->path }}</div><div>{{ $a->mime_type }}</div><form method="post" action="{{ route('admin.media.destroy',$a) }}" class="mt-2">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></div></div>@endforeach</div><div class="mt-3">{{ $assets->links() }}</div></div>@endsection
