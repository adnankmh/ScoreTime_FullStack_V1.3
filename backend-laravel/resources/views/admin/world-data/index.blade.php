@extends('layouts.app')
@section('content')
<div class="container py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1>Global Football Data</h1><p class="text-muted mb-0">Licensed provider catalog, countries, competitions, clubs, national teams and real players.</p></div><span class="badge {{($health['ok']??false)?'bg-success':'bg-danger'}}">{{($health['ok']??false)?'Provider healthy':'Provider unavailable'}}</span></div>
<div class="row g-3 mb-4">@foreach($counts as $label=>$value)<div class="col-6 col-lg-3"><div class="card p-3"><small class="text-muted">{{ucfirst($label)}}</small><strong class="fs-3">{{number_format($value)}}</strong></div></div>@endforeach</div>
<div class="card p-4 mb-4"><h3>Synchronize</h3><form method="POST" action="{{route('admin.world-data.sync')}}" class="row g-3">@csrf<div class="col-md-3"><label class="form-label">Scope</label><select class="form-select" name="scope"><option value="catalog">Countries + competitions</option><option value="countries">Countries only</option><option value="league">League teams + squads</option><option value="players">League player statistics</option></select></div><div class="col-md-2"><label class="form-label">Season</label><input class="form-control" name="season" value="{{date('Y')}}"></div><div class="col-md-2"><label class="form-label">Provider league ID</label><input class="form-control" name="league"></div><div class="col-md-2"><label class="form-label">Country</label><input class="form-control" name="country"></div><div class="col-md-1"><label class="form-label">Pages</label><input class="form-control" name="pages" value="10"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Run sync</button></div></form><div class="mt-3 small text-muted">API key stays server-side. Sync only data permitted by your provider plan and license.</div>@if(session('status'))<pre class="alert alert-success mt-3 mb-0">{{session('status')}}</pre>@endif @if(session('error'))<pre class="alert alert-danger mt-3 mb-0">{{session('error')}}</pre>@endif</div>
<div class="card p-4"><h3>Recently synchronized competitions</h3><div class="table-responsive"><table class="table"><thead><tr><th>Competition</th><th>Country</th><th>Type</th><th>Provider ID</th><th>Last sync</th></tr></thead><tbody>@foreach($recent as $c)<tr><td>{{$c->name_en}}</td><td>{{$c->country}}</td><td>{{$c->type}}</td><td>{{$c->provider_id}}</td><td>{{$c->last_synced_at}}</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
