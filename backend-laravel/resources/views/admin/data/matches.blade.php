@extends('layouts.app') @section('content')<div class="admin-head"><h1>{{__('ui.matches')}}</h1></div><div class="panel table-wrap"><table><thead><tr><th>Match</th><th>Competition</th><th>Kickoff</th><th>Live control</th></tr></thead><tbody>@foreach($items as $x)<tr><td>{{$x->homeTeam?->name_en}} vs {{$x->awayTeam?->name_en}}</td><td>{{$x->competition?->name_en}}</td><td>{{$x->kickoff_at}}</td><td><form method="post" action="{{route('admin.matches.update',$x)}}" style="display:flex;gap:6px">@csrf @method('PATCH')<select name="status">@foreach(['scheduled','live','halftime','finished','postponed','cancelled'] as $s)<option @selected($x->status===$s)>{{$s}}</option>@endforeach</select><input name="minute" type="number" value="{{$x->minute}}" style="width:75px"><input name="home_score" type="number" value="{{$x->home_score}}" style="width:65px"><input name="away_score" type="number" value="{{$x->away_score}}" style="width:65px"><button class="btn primary">{{__('ui.save')}}</button></form></td></tr>@endforeach</tbody></table></div>{{$items->links()}}@endsection