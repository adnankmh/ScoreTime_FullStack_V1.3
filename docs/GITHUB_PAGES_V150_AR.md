# ScoreTime V1.5.0 — GitHub Pages

الرابط المستهدف:
`https://adnankmh.github.io/ScoreTime/`

مرة واحدة فقط:
- اسم المستودع: `ScoreTime`
- Settings → Pages → Source → GitHub Actions

Workflow `ScoreTime • Web / GitHub Pages` يبني Flutter Web على `/ScoreTime/` وينشره تلقائياً.
إذا لم تضبط `SCORETIME_API_BASE_URL` يعمل الموقع في Professional Demo Mode. عند توفير Laravel API عام، ضع المتغير في Settings → Secrets and variables → Actions → Variables.
