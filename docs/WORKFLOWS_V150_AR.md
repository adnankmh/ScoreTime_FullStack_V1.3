# ScoreTime V1.5.0 — Workflows

يوجد 4 Workflows فقط:
1. `ScoreTime • Laravel` — MySQL + migrations + PHPUnit.
2. `ScoreTime • Web / GitHub Pages` — Flutter Web + Pages.
3. `ScoreTime • Android APK + AAB` — يبني APK وAAB معاً.
4. `ScoreTime • iOS` — يبني iOS Release بدون توقيع.

ملفات Workflows القديمة ليست موجودة كملفات YAML فعالة داخل الأرشيف.
